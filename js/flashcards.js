// Flashcard reviewer — built next
document.addEventListener('DOMContentLoaded', () => {
  document.querySelector('#fc-root .subtitle').textContent =
    'Flashcard mode coming in the next build.';
});
